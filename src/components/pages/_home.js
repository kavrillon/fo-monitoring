import Controller from './Controller';

export default class HomeController extends Controller {
    constructor() {
        super(data);

        this.data = data;
        this.init(data);
    }

    init() {

    }
}
